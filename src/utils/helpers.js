/*
 * Folder: src/utils/
 * Purpose: This folder contains common pure utility functions, constants, validation schemas, and general helpers.
 */

// Simple placeholder helper functions
export const formatName = (name) => {
  return name ? name.trim() : '';
};
